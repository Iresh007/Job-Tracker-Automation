kind = "web"
previewPath = "/"
title = "ApplyBlitz"
version = "1.0.0"
id = "artifacts/apply-blitz"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18128

[services.development]
run = "pnpm --filter @workspace/apply-blitz run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/apply-blitz", "run", "build" ]
publicDir = "artifacts/apply-blitz/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18128"
BASE_PATH = "/"
